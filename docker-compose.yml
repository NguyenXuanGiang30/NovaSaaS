version: '3.8'

services:
  # PostgreSQL with pgvector (nếu bạn muốn dùng container thay vì local)
  # postgres:
  #   image: pgvector/pgvector:pg16
  #   container_name: novas_ai_db
  #   environment:
  #     POSTGRES_USER: postgres
  #     POSTGRES_PASSWORD: giang2005
  #     POSTGRES_DB: NovaSaaS_Db
  #   ports:
  #     - "5432:5432"
  #   volumes:
  #     - postgres_data:/var/lib/postgresql/data

  # Redis for caching
  redis:
    image: redis:7-alpine
    container_name: novas_redis
    ports:
      - "6379:6379"
    volumes:
      - redis_data:/data
    command: redis-server --appendonly yes
    restart: unless-stopped

volumes:
  # postgres_data:
  redis_data:
